import { Box } from '@material-ui/core'
import React, { ReactElement } from 'react'

export const TournamentsPage = (): ReactElement => {
	return (
		<Box color="white">
            TUT BUDUT TIRIKI
		</Box>
	)
}